#!/usr/bin/env bash
set -Eeuo pipefail
printf '%s\n' 'NetBox configuration generation is intentionally out of scope for this manager.' \
  'Maintain the existing deployment configuration with its installation tooling.' >&2
exit 2
